//go:build windows

// Package jsonui provides a JSON-based declarative UI loader for widgets.Scene.
//
// JSON UI documents define window metadata, widget trees, styles, actions,
// bindings, absolute expressions, and multi-window application layouts without
// embedding data mutation logic inside the JSON text itself.
package jsonui
